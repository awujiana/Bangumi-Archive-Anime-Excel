# 项目操作指南

本指南是 `github-data-distribution.md` 的操作落地手册，面向仓库维护者，覆盖初始化、数据发布、验证、维护的完整流程。

> 配套文档：[`github-data-distribution.md`](./github-data-distribution.md)（数据下发机制说明）

---

## 一、仓库定位

本仓库作为 **BGM 插件的数据下发目录**，向 WPS Excel 加载项提供 Bangumi 条目数据。

```
插件 ──读取──▶ latest.json ──解析──▶ browser_download_url ──下载──▶ zip ──解压──▶ JSONL ──写入──▶ Excel
```

### 1.1 当前仓库结构

```
Bangumi-Archive-Anime-Excel/
├── github-data-distribution.md   ← 数据下发机制说明（已存在）
├── 项目操作指南.md                ← 本文档
├── dump/                          ← Bangumi 原始 dump（合计约 65 MB）
│   ├── dump-2026-07-14/
│   │   ├── type2_episode.jsonlines
│   │   ├── type2_subject-relations.jsonlines
│   │   └── type2_subject.jsonlines
│   └── dump-2026-07-21/
│       ├── type2_episode.jsonlines       (0.09 MB)
│       ├── type2_subject-relations.jsonlines  (6.30 MB)
│       └── type2_subject.jsonlines       (58.84 MB)
└── differences/                   ← 周期 diff 报告
    ├── 2026-07-07_to_2026-07-14/
    └── 2026-07-14_to_2026-07-21/
```

### 1.2 文档要求 vs 仓库现状

| 文档要求 | 仓库现状 | 处理建议 |
|---|---|---|
| `data/bangumi.jsonl`（直接模式） | ✅ 已存在（~15.80 MB，30502 条） | **当前启用模式**，dump 合计 ~65 MB < 100 MB 阈值，直接 JSONL 模式即可 |
| 根目录有 `latest.json` | ❌ 缺失 | 仅在切换 Release Asset 模式时才需要 |
| Release Asset zip 包 | ❌ 未创建 | 数据 <100 MB，无需此模式 |
| 仓库为 public | 需确认 | 在 GitHub 仓库 Settings → General 中确认 |
| JSONL 字段：25 个（3 基础 + 3 顶层扩展 + 19 infobox 扩展） | ✅ 已实现 | 字段映射规则见 [scripts/README.md](./scripts/README.md) |

### 1.3 模式选型

依据 `github-data-distribution.md` 第 158-166 行的对比表：

| 数据规模 | 推荐模式 | 本仓库情况 |
|---|---|---|
| < 100 MB | 直接 JSONL 模式 | ✅ dump 合计 ~65 MB，**采用此模式** |
| ≥ 100 MB | Release Asset 模式 | 不适用 |

本仓库数据规模稳定在 100 MB 以下，**默认采用直接 JSONL 模式**（path = `data/bangumi.jsonl`），无需创建 Release 与 `latest.json`。如未来数据增长超过 100 MB，再按第五章切换 Release Asset 模式。

---

## 二、环境与前置条件

### 2.1 工具准备

| 工具 | 用途 | 验证命令 |
|---|---|---|
| `git` | 仓库管理 | `git --version` |
| `gh` CLI | 创建 Release、上传 Asset、调用 API | `gh auth status` |
| `zip` | 打包数据 | `zip --version` |
| `sha256sum` | 生成校验值 | `sha256sum --version` |
| `jq` | 解析 GitHub API 响应 | `jq --version` |

### 2.2 一次性配置

```bash
# 1. 克隆仓库（如尚未克隆）
git clone https://github.com/{owner}/{repo}.git
cd {repo}

# 2. 登录 gh CLI
gh auth login

# 3. 确认仓库可见性为 public
gh repo view --json visibility -q '.visibility'
```

---

## 三、发布新版本流程（直接 JSONL 模式，推荐）

适用于本仓库当前数据规模（dump 合计约 65 MB，远小于 100 MB 阈值）。无需创建 Release 和 `latest.json`，直接将 JSONL 提交到仓库即可。

### 3.1 第一步：字段映射与数据准备

dump 目录中的 `type2_subject.jsonlines` 字段与插件要求的 JSONL 格式不一致，**必须先转换**。

完整的字段映射规则、转换脚本使用说明、Python 脚本实现规范，详见 [`scripts/README.md`](./scripts/README.md)。

#### 字段映射概要

输出字段共 25 个，分 3 类：

| 类别 | 字段数 | 字段列表 |
|---|---|---|
| 基础字段 | 3 | `sid`、`name`、`updatedAt`（YYYY-MM-DD） |
| dump 顶层扩展 | 3 | `date`、`meta_tags`、`nsfw` |
| infobox 解析扩展 | 19 | `话数`、`播放结束`、`动画制作公司`、`片长`、`制片国家`、`语言`、`类型`、`导演`、`音乐`、`人物设定`、`机械设定`、`原作`、`脚本`、`分镜`、`演出`、`原案`、`系列构成`、`在线播放平台`、`关联的动漫ID` |

不输出字段（已移除以减少冗余）：`url`（从 sid 构造）、`type`（恒为 2）、`tags`（与 meta_tags 冗余）、`episode`（恒为 0）、`platform`、`score`（数据源平均分）、`comment`（dump 不含）。

> ⚠️ **注意**：`type` 字段在 dump 中表示条目类型（2=动画），在 BGM 插件 → Bangumi API 中表示观看状态（1=想看 2=在看 3=看过 4=搁置 5=抛弃），两者完全无关。自 2026-07-30 起不再输出 `type` 字段。

#### 使用转换脚本

仓库已提供 Python 版本（唯一实现，跨平台无外部依赖）：

```bash
# 基本用法（在仓库根目录执行，仅依赖 Python 3.10+ 标准库）
python scripts/convert_dump_to_jsonl.py 2026-07-21

# 自定义路径
python scripts/convert_dump_to_jsonl.py 2026-07-21 --dump-dir ./dump --out-file ./data/bangumi.jsonl
```

脚本会读取 `dump/dump-2026-07-21/type2_subject.jsonlines` + `type2_subject-relations.jsonlines`，转换后输出到 `data/bangumi.jsonl`，UTF-8 无 BOM，LF 换行。

### 3.2 第二步：提交并推送 JSONL

```bash
# 在仓库根目录执行
git add data/bangumi.jsonl
git commit -m "chore: update bangumi.jsonl → dump-$(date -u +%Y-%m-%d)"
git push origin main
```

### 3.3 第三步：插件端配置

| 配置项 | 值 |
|---|---|
| 同步源 | GitHub |
| owner | `{你的用户名}` |
| repo | `{你的仓库名}` |
| branch | `main` |
| path | `data/bangumi.jsonl` |

插件会从以下 URL 直接读取 JSONL：

```
https://raw.githubusercontent.com/{owner}/{repo}/main/data/bangumi.jsonl
```

> ⚠️ **限流提醒**：raw URL 未认证读取限 60 次/小时/IP（文档第 166 行）。如遇限流，可等待 1 小时或切换 Release Asset 模式（不限流）。

---

## 四、验证流程

### 4.1 静态校验

```bash
# 1. JSONL 可被 raw URL 访问
curl -fsSL "https://raw.githubusercontent.com/{owner}/{repo}/main/data/bangumi.jsonl" | head -1

# 2. 每行可被 jq 解析
curl -fsSL "https://raw.githubusercontent.com/{owner}/{repo}/main/data/bangumi.jsonl" | head -10 | jq -c .

# 3. 字段完整性检查（sid/name 必填）
curl -fsSL "https://raw.githubusercontent.com/{owner}/{repo}/main/data/bangumi.jsonl" \
  | jq -c 'select(.sid == null or .name == null) | "INVALID: \(.)"' \
  | head -5
```

### 4.2 插件端验证

1. 打开 WPS Excel → 加载项面板
2. 配置同步源（参见 3.3）
3. 点击「GitHub 同步」
4. 预期行为：
   - 读取 `https://raw.githubusercontent.com/{owner}/{repo}/main/data/bangumi.jsonl`
   - 解析 JSONL → 写入 Excel

---

## 五、Release Asset 模式（大文件备选）

仅当数据增长到 ≥100 MB 或需要版本历史/不限流下载时启用。本仓库当前无需此模式。

### 5.1 打包为 zip

```bash
# 在仓库根目录执行
zip data.zip data/bangumi.jsonl

# 确认包内只有一个 JSONL 文件（文档第 173 行要求）
unzip -l data.zip
```

### 5.2 计算 SHA-256

```bash
sha256sum data.zip
# 输出形如：e1120169088407c66a94dacacda4dffaabe0e2e08cbcc8238c880f6c0140dd57  data.zip
```

记录 hash 值（去掉文件名部分），后续填入 `digest` 字段，格式为 `sha256:{hash}`。

### 5.3 创建 GitHub Release 并上传 Asset

```bash
TAG="v$(date -u +%Y-%m-%d)"
TITLE="数据更新 $(date -u +%Y-%m-%d)"

# 创建 tag 并推送
git tag -a "$TAG" -m "数据更新 $(date -u +%Y-%m-%d)"
git push origin "$TAG"

# 创建 Release 并上传 asset
gh release create "$TAG" data.zip \
  --title "$TITLE" \
  --notes "最新 bangumi 数据 dump，来源 dump/dump-$(date -u +%Y-%m-%d)/"
```

### 5.4 更新 latest.json

```bash
# 从 GitHub API 获取刚上传的 Asset 信息，直接写入 latest.json
gh api repos/{owner}/{repo}/releases/latest --jq '.assets[0]' > latest.json

# 校验文件内容
cat latest.json | jq .
```

`latest.json` 应包含以下字段（参见文档第 50-63 行）：

- `browser_download_url`（核心，插件实际下载地址）
- `content_type`、`created_at`、`digest`、`id`、`label`、`name`、`size`、`updated_at`、`url`

> ⚠️ GitHub API 返回的 `digest` 字段可能为空，需手动用 5.2 计算的 SHA-256 填入：`sha256:{hash}`。

### 5.5 提交并推送 latest.json

```bash
git add latest.json
git commit -m "chore: update latest.json → $TAG"
git push origin main
```

### 5.6 切换插件配置

将插件端 `path` 从 `data/bangumi.jsonl` 改为 `latest.json`。

---

## 六、周期性维护

### 6.1 每周例行（数据更新）

```bash
# 1. 拉取最新 dump 到 dump/dump-{new-date}/
# 2. 生成 diff 报告到 differences/{old}_to_{new}/
# 3. 执行第三、四章完整流程
```

### 6.2 历史数据归档

`dump/` 目录下保留最近 2-3 个版本即可，更早的可清理或迁出到归档分支：

```bash
# 示例：清理 2026-07-07 之前的 dump
git rm -r dump/dump-2026-07-07/
git commit -m "chore: archive old dump 2026-07-07"
```

### 6.3 检查清单（直接 JSONL 模式）

- [ ] `data/bangumi.jsonl` 存在且字段格式符合 3.1 转换规则
- [ ] 仓库可见性为 public
- [ ] 插件端配置 `path = data/bangumi.jsonl`
- [ ] raw URL 可访问：`https://raw.githubusercontent.com/{owner}/{repo}/main/data/bangumi.jsonl`
- [ ] 未在根目录残留 `data.jsonl`（避免与 `data/bangumi.jsonl` 混淆）
- [ ] 如已切换 Release Asset 模式：`latest.json` 存在且 `browser_download_url` 指向最新 Release，`digest` 已正确填充 `sha256:{hash}`

---

## 七、故障排查

| 现象 | 可能原因 | 处理 |
|---|---|---|
| 插件读取 JSONL 404 | 仓库非 public / path 错误 / 分支错误 | 检查仓库可见性、branch、path 配置（应为 `data/bangumi.jsonl`） |
| JSONL 解析报错 | 字段未转换 / dump 直接提交 | 按 3.1 转换字段后再提交 |
| 限流（60次/小时） | raw URL 未认证读取 | 等待 1 小时，或切换 Release Asset 模式（不限流） |
| 下载失败（Release 模式） | Release 为 private / tag 误删 | 重新创建 Release 并上传 asset |
| SHA-256 校验失败（Release 模式） | zip 内容被修改 / digest 未更新 | 重新打包并更新 digest |

---

## 八、回滚

### 8.1 直接 JSONL 模式回滚

利用 git 历史回退到上一版本的数据文件：

```bash
# 1. 查看提交历史，定位上一版本的 commit
git log --oneline -- data/bangumi.jsonl

# 2. 回退到指定版本（保留工作区其他改动）
git checkout {previous-commit} -- data/bangumi.jsonl

# 3. 提交推送
git add data/bangumi.jsonl
git commit -m "chore: rollback data/bangumi.jsonl → {previous-commit}"
git push origin main
```

### 8.2 Release Asset 模式回滚

```bash
# 1. 查看历史 Release
gh release list

# 2. 将 latest.json 指向上一个版本
gh api repos/{owner}/{repo}/releases --jq '.[1].assets[0]' > latest.json
# 手动补全 digest 字段

# 3. 提交推送
git add latest.json
git commit -m "chore: rollback latest.json → {previous-tag}"
git push origin main
```
