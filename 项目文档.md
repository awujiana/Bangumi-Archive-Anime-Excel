# Bangumi-Archive-Anime-Excel 项目文档

> **架构变更说明（2026-08-05）**：
> - `scripts/` 目录已删除，转换脚本 `convert_dump_to_jsonl.py` 迁移到上游 Archive 项目（`d:\编程\awujiana\Archive\scripts\`）
> - `.github/workflows/convert.yml` 已删除，dump→JSONL 转换由 Archive 项目本地执行后同步到本仓库 `data/` 目录
> - 本仓库现仅作为**数据下发仓库**，存放 `data/bangumi.jsonl` 和 `differences/` 差异摘要
> - 下文中涉及 `scripts/` 和 `convert.yml` 的章节为历史参考，不再反映当前架构
> - 字段映射规则的权威来源变更为 Archive 项目的 `scripts/convert_dump_to_jsonl.py` 中的 `PluginRecord` TypedDict

> **文档定位**：本文件是 `Bangumi-Archive-Anime-Excel` 仓库的**总入口文档**，汇总仓库定位、跨项目关系、目录结构、数据下发机制、维护规则与文档索引。
> **权威性**：本文件描述仓库级架构与流程。`latest.json` 字段格式以 [github-data-distribution.md](./github-data-distribution.md) 为权威；转换脚本接口以 Archive 项目 `scripts/convert_dump_to_jsonl.py` 为权威。
> **最后核实**：2026-08-05
> **核实节奏**：每次新增 dump 目录或 Archive 项目修改 `convert_dump_to_jsonl.py` 字段映射后更新；至少每 4 周复核一次。
> **陈旧信号**：若 `data/bangumi.jsonl` 首行 `updatedAt` 与 Archive 项目最新 dump 日期不一致，或字段集与 Archive 项目 `scripts/convert_dump_to_jsonl.py` 中 `PluginRecord` 不一致，本文件视为 `stale`。

---

## 一、项目定位

**受众**：仓库维护者；WPS Excel 加载项（BGM 插件）用户；下游插件开发者。

**职责**：
1. 作为 **BGM 插件的数据下发源**，通过 GitHub raw URL 向 WPS Excel 加载项提供 Bangumi 条目数据。
2. 接收上游 `Archive` 项目同步来的原始 dump 数据与差异报告。
3. 通过 GitHub Actions 自动将 `type2_subject.jsonlines` 转换为插件所需的紧凑 JSONL 格式（`data/bangumi.jsonl`）。
4. 保留历史 dump 与差异报告，支持版本回溯与增量分析。

**GitHub 仓库**：[awujiana/Bangumi-Archive-Anime-Excel](https://github.com/awujiana/Bangumi-Archive-Anime-Excel)（必须为 public）

**当前模式**：**直接 JSONL 模式**（数据规模 ~15.80 MB < 100 MB 阈值，无需 Release Asset 模式）

**技术栈**：Python 3.10+（转换脚本，仅标准库 + 同目录 `wiki_subject_parser.py`） + GitHub Actions（CI/CD）

---

## 二、跨项目关系

本仓库是 **三项目数据链路的中转枢纽**：

```
Bangumi 官方 Archive (周三凌晨 dump)
        ↓ 每周下载
[1] Archive 项目  ── 处理 dump、生成 Excel 与差异
        │
        │ scripts/sync_to_awujiana.py
        ↓ 复制 dump/*.jsonlines + Difference/ 到本仓库
[2] Bangumi-Archive-Anime-Excel (本仓库)  ── 数据下发仓库
        │
        │ GitHub Actions convert.yml (push 到 dump/** 触发)
        ↓ 自动转 data/bangumi.jsonl
[3] Excel 加载项  ── WPS 插件读取 raw URL JSONL → 写入 Excel → 同步 Bangumi API
```

| 上游 | 接收内容 | 触发方式 |
|---|---|---|
| `Archive` 项目 | `dump/dump-{date}/type2_*.jsonlines` + `differences/{old}_to_{new}/` | 上游手动执行 `python scripts/sync_to_awujiana.py --type 2` |

| 下游 | 提供内容 | 访问方式 |
|---|---|---|
| `Excel 加载项` | `data/bangumi.jsonl`（紧凑 JSONL，~15.80 MB，30502 条记录） | `https://raw.githubusercontent.com/awujiana/Bangumi-Archive-Anime-Excel/main/data/bangumi.jsonl` |

详细跨项目链路见上级目录 [../README.md](../README.md)。

---

## 三、目录结构

```
Bangumi-Archive-Anime-Excel/
├── .github/
│   └── workflows/
│       └── convert.yml              # CI: dump/** 变更自动转 bangumi.jsonl
├── data/
│   └── bangumi.jsonl                # 插件读取的数据源（紧凑 JSONL，~15.80 MB，30502 条）
├── dump/                            # Bangumi 原始 dump（合计约 65 MB）
│   ├── dump-2026-07-14/
│   │   ├── type2_episode.jsonlines
│   │   ├── type2_subject-relations.jsonlines
│   │   └── type2_subject.jsonlines
│   └── dump-2026-07-21/
│       ├── type2_episode.jsonlines       (0.09 MB)
│       ├── type2_subject-relations.jsonlines  (6.30 MB)
│       └── type2_subject.jsonlines       (58.84 MB)
├── differences/                     # 周期 diff 报告
│   ├── 2026-07-07_to_2026-07-14/
│   └── 2026-07-14_to_2026-07-21/
├── scripts/
│   ├── README.md                    # 转换脚本说明（字段映射、用法、Python 规范）
│   ├── convert_dump_to_jsonl.py     # Python 版转换脚本（唯一实现，跨平台无外部依赖）
│   └── wiki_subject_parser.py       # infobox 解析器（来自 Archive 项目，360 字段映射）
├── .gitignore
├── github-data-distribution.md      # 数据下发机制说明（latest.json 格式权威）
├── 项目操作指南.md                  # 仓库维护者操作手册
└── 项目文档.md                      # 本文件
```

### 3.1 differences/ 目录结构（每个周期）

```
differences/{old-date}_to_{new-date}/
├── diff_report.html                                  # 差异对比可视化 HTML 报告
├── diff_summary.json                                 # 差异统计汇总 JSON
├── type2_subject_{old}_vs_{new}_added.jsonlines      # 新增条目
├── type2_subject_{old}_vs_{new}_deleted.jsonlines    # 删除条目
├── type2_subject_{old}_vs_{new}_modified.json        # 修改条目（含字段变更详情）
├── type2_subject-relations_{old}_vs_{new}_added.jsonlines
├── type2_subject-relations_{old}_vs_{new}_deleted.jsonlines
├── type2_subject-relations_{old}_vs_{new}_modified.jsonlines
├── type2_subject_{old}_vs_{new}.xlsx                 # 差异 Excel（6 Sheet）
└── 差异数据_type2_{old}_vs_{new}.html                # 差异 Excel 的 HTML 预览
```

---

## 四、核心机制

### 4.1 数据下发流程（直接 JSONL 模式）

```
插件 ──① 读取──▶ data/bangumi.jsonl (raw URL)
                   │
                   │ ② 逐行解析 JSONL
                   ▼
            ③ 按 SID 比对本地 Excel 行
                   │
                   │ ④ 增量写入（新增/更新/删除）
                   ▼
            ⑤ 写入 Excel 单元格
```

**插件读取 URL**：
```
https://raw.githubusercontent.com/awujiana/Bangumi-Archive-Anime-Excel/main/data/bangumi.jsonl
```

**限流**：未认证 raw URL 读取限 60 次/小时/IP。如遇限流，等待 1 小时或切换 Release Asset 模式。

### 4.2 CI/CD 自动转换流程（GitHub Actions）

工作流文件：[.github/workflows/convert.yml](./.github/workflows/convert.yml)

```
你 push 包含 dump/dump-{date}/ 的 commit 到 main
              ↓
GitHub Actions 触发（paths: dump/** 过滤）
              ↓
自动检测最新 dump 目录（dump-YYYY-MM-DD）
              ↓
检查 data/bangumi.jsonl 的 updatedAt
              ↓ 如已有该日期则跳过
执行转换脚本（python scripts/convert_dump_to_jsonl.py）
              ↓
git commit + push data/bangumi.jsonl
              ↓
插件下次同步即可拉取新数据
```

| 触发方式 | 条件 | 说明 |
|---|---|---|
| 自动触发 | push 到 `main` 且 `dump/**` 路径有变更 | 仅 dump 目录变更才触发，data/ 或其他目录变更不会触发 |
| 手动触发 | Actions 页面点击 "Run workflow" | 可选指定 `dump_date` 参数，留空则自动检测最新 |

**循环触发防护**：
- workflow 推送的 commit 只包含 `data/bangumi.jsonl`（不在 `dump/**` 路径下），不会再次触发
- "Check if already converted" 步骤检测 `data/bangumi.jsonl` 的 `updatedAt` 是否已是该日期，是则跳过
- GitHub 官方策略：`GITHUB_TOKEN` 推送的 commit 默认不触发新 workflow

**Commit 格式**：
```
chore: update bangumi.jsonl -> dump-2026-07-28

自动转换自 dump/dump-2026-07-28/type2_subject.jsonlines
触发来源: push
工作流: 12345678901
```
作者：`github-actions[bot] <github-actions[bot]@users.noreply.github.com>`

### 4.3 字段映射（dump → 插件 JSONL）

dump 文件字段与插件 JSONL 字段不一致，必须按以下规则转换。完整字段映射规则、类型守卫、运行时校验详见 [`scripts/README.md`](./scripts/README.md) 与 [`scripts/convert_dump_to_jsonl.py`](./scripts/convert_dump_to_jsonl.py) 中的 `PluginRecord` TypedDict 定义。

#### 输出字段（25 个，3 类）

**基础字段**（3 个，BGM 插件 `GithubRemoteEntry` 接口）：

| dump 字段 | 插件字段 | 类型 | 转换规则 |
|---|---|---|---|
| `id` | `sid` | string | 数字转字符串，如 `8` → `"8"`（主键，必填） |
| `name_cn` / `name` | `name` | string | 优先 `name_cn`；若为空字符串或 None，回退到 `name`；都为空则 `""` |
| （dump 日期） | `updatedAt` | string | 取 dump 日期，格式 `YYYY-MM-DD`（仅日期，不含时间，节省空间） |

**dump 顶层扩展**（3 个）：

| dump 字段 | 插件字段 | 类型 | 说明 |
|---|---|---|---|
| `date` | `date` | string | 开播日期 |
| `meta_tags` | `meta_tags` | string[] | 标签/分级（写入 Excel「标签/分级」列，`\|` 分隔） |
| `nsfw` | `nsfw` | bool | 布尔值 |

**infobox 解析扩展**（19 个，中文命名，由 `WikiSubjectParser` 解析 infobox 字符串得到）：

| infobox 字段 | 插件字段 | Excel 列名 | 说明 |
|---|---|---|---|
| `话数` | `话数` | `集数` | 集数（独立处理） |
| `播放结束` | `播放结束` | `完播日` | 完播日 |
| `动画制作`/`动画制作公司` | `动画制作公司` | `制作公司` | 制作公司（字段名重映射） |
| `片长` | `片长` | `时长` | 时长/分钟 |
| `制片国家` | `制片国家` | `制片国家` | 同名映射 |
| `语言` | `语言` | `语言` | 同名映射 |
| `类型` | `类型` | `类型` | 同名映射 |
| `导演` | `导演` | `导演` | 同名映射 |
| `音乐` | `音乐` | `音乐` | 同名映射 |
| `人物设定` | `人物设定` | `人物设定` | 同名映射 |
| `机械设定` | `机械设定` | `机械设定` | 同名映射 |
| `原作` | `原作` | `原作` | 同名映射 |
| `脚本` | `脚本` | `脚本` | 同名映射 |
| `分镜` | `分镜` | `分镜` | 同名映射 |
| `演出` | `演出` | `演出` | 同名映射 |
| `原案` | `原案` | `原案` | 同名映射 |
| `系列构成` | `系列构成` | `系列构成` | 同名映射 |
| `在线播放平台` | `在线播放平台` | `在线播放平台` | 同名映射 |
| `subject-relations` | `关联的动漫ID` | `关联的动漫ID` | 关联 subject_id 列表，`' \| '` 分隔 |

#### 不输出字段（已移除以减少冗余和内存占用）

| 字段 | 移除原因 |
|---|---|
| `url` | 从 `sid` 构造（`https://bgm.tv/subject/{sid}`），BGM 插件同步时自动构造，输出冗余 |
| `type` | dump 中恒为 `2`（动画条目类型），BGM 插件默认按 2 处理，输出冗余 |
| `tags` | 与 `meta_tags` 语义冗余，BGM 插件仅使用 `meta_tags` 写入「标签/分级」列 |
| `episode` | dump 中恒为 `0`（dump 不含观看集数），BGM 插件使用 infobox 的「话数」字段写入「集数」列 |
| `platform` | BGM 插件 `EXTENDED_FIELD_MAP` 已移除该字段映射，输出无意义 |
| `score` | dump 的 `score` 为数据源平均分（非用户打分），用户在 Excel「打分」列自行维护 |
| `comment` | dump 不含个人评论，用户在 Excel「剧透/感言」列自行维护 |

> ⚠️ **`type` 字段语义说明**：dump 中 `type:2` 表示 Bangumi 条目类型为「动画」（条目类型）；Excel 加载项调用 Bangumi API 时的 `type` 为「观看状态」（1=想看 2=在看 3=看过 4=搁置 5=抛弃），由用户在 Excel「是否看完」列维护。两者完全无关。自 2026-07-30 起，`data/bangumi.jsonl` 不再输出 `type` 字段。

### 4.4 JSONL 单条记录示例

```jsonl
{"sid":"8","name":"Code Geass 反叛的鲁路修R2","updatedAt":"2026-07-21","date":"2008-04-06","meta_tags":["机战","TV","日本","原创","战斗"],"nsfw":false,"播放结束":"2008年9月28日","动画制作公司":"サンライズ、david production、スタジオガッツ；作画协力：GONZO","话数":"25","片长":"","制片国家":"","语言":"","类型":"","导演":"谷口悟朗","音乐":"中川幸太郎、黒石ひとみ","人物设定":"木村貴宏","机械设定":"寺岡賢司、沙倉拓実；Knightmare设计：安田朗、中田栄治、阿久津潤一","原作":"","脚本":"","分镜":"","演出":"","原案":"故事原案：大河内一楼、谷口悟朗","系列构成":"大河内一楼","在线播放平台":"","关联的动漫ID":"85 | 344 | 793 | 1081 | 3219 | 8813"}
```

字段定义详见 [github-data-distribution.md](./github-data-distribution.md#字段定义) 与 [Excel 加载项 docs/github-data-distribution.md](../Excel加载项/BGM/BGM/docs/github-data-distribution.md#字段定义)。

---

## 五、使用方法

### 5.1 自动模式（推荐）

只需把新的 dump 数据 push 到仓库：

```bash
# 假设外部工具导出了 dump-2026-07-28
git add dump/dump-2026-07-28/
git commit -m "chore: add dump-2026-07-28"
git push origin main

# GitHub Actions 会自动：
# 1. 检测到 dump/dump-2026-07-28/
# 2. 执行转换脚本
# 3. 提交并推送 data/bangumi.jsonl
```

### 5.2 手动模式（在 Actions 页面）

1. 进入 GitHub 仓库的 **Actions** 页面
2. 选择 "Convert Dump to JSONL" workflow
3. 点击 "Run workflow"
4. 可选：输入 `dump_date` 参数（如 `2026-07-28`），留空则自动检测最新
5. 点击绿色 "Run workflow" 按钮

### 5.3 本地手动转换（调试用）

```bash
# 在仓库根目录执行（仅依赖 Python 3.10+ 标准库）
python scripts/convert_dump_to_jsonl.py 2026-07-21

# 自定义路径
python scripts/convert_dump_to_jsonl.py 2026-07-21 --dump-dir ./dump --out-file ./data/bangumi.jsonl
```

### 5.4 验证流程

```bash
# 1. JSONL 可被 raw URL 访问
curl -fsSL "https://raw.githubusercontent.com/awujiana/Bangumi-Archive-Anime-Excel/main/data/bangumi.jsonl" | head -1

# 2. 每行可被 jq 解析
curl -fsSL "https://raw.githubusercontent.com/awujiana/Bangumi-Archive-Anime-Excel/main/data/bangumi.jsonl" | head -10 | jq -c .

# 3. 字段完整性检查（sid/name 必填）
curl -fsSL "https://raw.githubusercontent.com/awujiana/Bangumi-Archive-Anime-Excel/main/data/bangumi.jsonl" \
  | jq -c 'select(.sid == null or .name == null) | "INVALID: \(.)"' \
  | head -5
```

---

## 六、Release Asset 模式（备选，当前未启用）

仅当数据增长到 ≥100 MB 或需要版本历史/不限流下载时启用。本仓库当前数据规模 ~15.80 MB，**无需此模式**。

切换步骤详见 [项目操作指南.md 第五章](./项目操作指南.md#五release-asset-模式大文件备选)。

### 两种模式对比

| 特性 | 直接 JSONL 模式（当前启用） | Release Asset 模式（预留） |
|---|---|---|
| 适用场景 | 小文件（< 100MB） | 大文件（> 100MB） |
| path 配置 | `data/bangumi.jsonl` | `latest.json` |
| 数据格式 | 直接 JSONL | zip 压缩包（内含 JSONL） |
| 版本管理 | 无（直接覆盖） | 有（Release tag 历史） |
| 下载方式 | raw.githubusercontent.com | github.com/releases/download |
| 限流 | 60 次/小时（未认证） | Release 下载不限流 |

> **注意**：当前插件代码仅实现了直接 JSONL 模式。Release Asset 模式（zip 下载+解压）需在 `ts/sync/githubSync.ts` 中新增实现。

---

## 七、维护与更新规则

### 7.1 更新触发器

| 触发事件 | 必须更新项 |
|---|---|
| 新增 `dump/dump-{date}/` 目录 | 本文件"目录结构"章节；CI 自动触发转换 |
| 修改 `scripts/convert_dump_to_jsonl.py` 字段映射 | [scripts/README.md](./scripts/README.md) 字段映射表 + 本文件"字段映射"章节 |
| 修改 `.github/workflows/convert.yml` 触发条件 | 本文件"CI/CD 自动转换流程"章节 |
| 切换到 Release Asset 模式 | 本文件"当前模式"标识 + [项目操作指南.md](./项目操作指南.md) |
| 插件端字段定义变更 | [github-data-distribution.md](./github-data-distribution.md) 字段定义表 + [Excel 加载项 schemas/githubEntry.ts](../Excel加载项/BGM/BGM/ts/schemas/githubEntry.ts) |

### 7.2 每周例行维护（与 Bangumi 官方 dump 节奏对齐）

```bash
# 1. 上游 Archive 项目执行 sync_to_awujiana.py 后，本仓库 dump/ 与 differences/ 已更新
# 2. 提交并推送（触发 CI 自动转换）
git add dump/dump-{new-date}/ differences/{old}_to_{new}/
git commit -m "chore: add dump-{new-date} and diff {old}_to_{new}"
git push origin main

# 3. CI 自动执行后，验证 data/bangumi.jsonl 已更新
curl -fsSL "https://raw.githubusercontent.com/awujiana/Bangumi-Archive-Anime-Excel/main/data/bangumi.jsonl" | head -1 | jq -r '.updatedAt'
# 应输出: "{new-date}"（仅日期，如 "2026-07-28"）
```

### 7.3 历史数据归档

`dump/` 目录下保留最近 2-3 个版本即可，更早的可清理或迁出到归档分支：

```bash
# 示例：清理 2026-07-07 之前的 dump
git rm -r dump/dump-2026-07-07/
git commit -m "chore: archive old dump 2026-07-07"
```

### 7.4 回滚

#### 直接 JSONL 模式回滚

```bash
# 1. 查看提交历史，定位上一版本的 commit
git log --oneline -- data/bangumi.jsonl

# 2. 回退到指定版本（保留工作区其他改动）
git checkout {previous-commit} -- data/bangumi.jsonl

# 3. 提交推送
git add data/bangumi.jsonl
git commit -m "chore: rollback data/bangumi.jsonl → {previous-commit}"
git push origin main
```

### 7.5 检查清单（直接 JSONL 模式）

- [ ] `data/bangumi.jsonl` 存在且字段格式符合字段映射规则
- [ ] 仓库可见性为 public
- [ ] 插件端配置 `path = data/bangumi.jsonl`
- [ ] raw URL 可访问：`https://raw.githubusercontent.com/awujiana/Bangumi-Archive-Anime-Excel/main/data/bangumi.jsonl`
- [ ] 未在根目录残留 `data.jsonl`（避免与 `data/bangumi.jsonl` 混淆）
- [ ] 如已切换 Release Asset 模式：`latest.json` 存在且 `browser_download_url` 指向最新 Release，`digest` 已正确填充 `sha256:{hash}`

### 7.6 故障排查

| 现象 | 可能原因 | 处理 |
|---|---|---|
| 插件读取 JSONL 404 | 仓库非 public / path 错误 / 分支错误 | 检查仓库可见性、branch、path 配置（应为 `data/bangumi.jsonl`） |
| JSONL 解析报错 | 字段未转换 / dump 直接提交 | 按字段映射规则转换后再提交 |
| 限流（60次/小时） | raw URL 未认证读取 | 等待 1 小时，或切换 Release Asset 模式（不限流） |
| CI 未触发 | push 的 commit 未修改 `dump/**` 路径 | 确认 dump 目录有变更，或手动 Run workflow |
| CI 转换失败 | dump 目录命名格式错误 / `type2_subject.jsonlines` 缺失 / Python 脚本报错 | 查看 Actions 日志，按错误信息修复 |

---

## 八、文档索引（Diátaxis 分类）

| 文档 | 类型 | 受众 | 用途 | 来源权威 |
|---|---|---|---|---|
| `项目文档.md`（本文件） | Reference | 仓库维护者 + 插件开发者 | 总入口、跨项目关系、CI 流程、目录索引 | 仓库级架构 |
| `github-data-distribution.md` | Reference | 插件开发者 + 仓库维护者 | `latest.json` 字段格式、两种下发模式对比 | 数据下发机制权威 |
| `项目操作指南.md` | How-to | 仓库维护者 | 初始化、发布、验证、维护、回滚完整流程 | 操作步骤权威 |
| `scripts/README.md` | Reference + How-to | 开发者 | 转换脚本接口、字段映射、Python 版规范 | 脚本接口权威 |
| `.github/workflows/convert.yml` | Reference | CI 维护者 | GitHub Actions 工作流定义 | CI 行为权威 |
| `../Excel加载项/BGM/BGM/docs/github-data-distribution.md` | Reference | 插件开发者 | 插件端视角的下发机制（含实际数据示例） | 插件端配置权威 |

---

## 九、环境与前置条件

### 9.1 工具准备

| 工具 | 用途 | 验证命令 |
|---|---|---|
| `git` | 仓库管理 | `git --version` |
| `gh` CLI | 创建 Release、上传 Asset、调用 API（Release 模式才需要） | `gh auth status` |
| `zip` | 打包数据（Release 模式才需要） | `zip --version` |
| `sha256sum` | 生成校验值（Release 模式才需要） | `sha256sum --version` |
| `jq` | 本地手动转换 | `jq --version` |

### 9.2 一次性配置

```bash
# 1. 克隆仓库（如尚未克隆）
git clone https://github.com/awujiana/Bangumi-Archive-Anime-Excel.git
cd Bangumi-Archive-Anime-Excel

# 2. 登录 gh CLI（Release 模式才需要）
gh auth login

# 3. 确认仓库可见性为 public
gh repo view --json visibility -q '.visibility'
```

---

## 十、联系方式

数据集疑问：`bingshanlengtie@qq.com`
Bangumi 官方：https://bgm.tv/
上游数据来源：https://github.com/bangumi/Archive/releases/tag/archive

许可协议：CC BY-NC-SA 4.0（详见上游 [Archive/type2-data-README.md](../Archive/type2-data-README.md#许可协议详情)）
